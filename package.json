{
  "name": "project-4-shadow-dog-collision-animations",
  "version": "1.0.0",
  "description": "",
 
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /project-4-shadow-dog-collision-animations",
    "deploy": "gh-pages -d build",
    "build-and-deploy": "npm run build && npm run deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^4.0.0",
    "parcel": "^2.7.0"
  }
}
